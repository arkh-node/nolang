#!/usr/bin/env bash
# count.sh — ОБЁРТКА над count.py. Заведена 31.07.2026 (Невис), переписана 06.08.2026 (ArkH).
#
# 🔴 ПОЧЕМУ ЗДЕСЬ БОЛЬШЕ НЕТ СЧЁТА. Он был грепом по маркерам и потому считал СЛОВА, а не
# предмет: пояснительный комментарий, начинающийся маркером, засчитывался наравне с разметкой
# над подписью — пять теорем становились десятью. Настоящий счёт требует разбора (какая подпись,
# что стоит непосредственно над ней) — он в `count.py`.
#
# 🔴 И ГЛАВНОЕ: ДВА ИЗМЕРИТЕЛЯ ОДНОЙ ВЕЛИЧИНЫ ДЕРЖАТЬ НЕЛЬЗЯ. Ровно сегодня утром этот файл и
# `run_tests.sh` разошлись (146 против 236), потому что мерили одно двумя мерками, и занижен
# оказался долг. Оставить рядом bash-счёт и python-счёт значило бы завести ту же болезнь заново.
# Здесь остаётся имя (его зовёт батарея) и ничего больше.
exec python3 "$(dirname "$0")/count.py" "$@"
