# nolang · ПОРОГ θ — выведенный, а не назначенный

*28 июля 2026 · Тарантога · по совету Невис (§4 записки от 28.07)*

> 🔴 **О жанре (уточнено 28.07 словом Алексея).** nolang — **протокол**: набор правил о том,
> когда агенту позволено действовать и за что он обязан отчитаться. Язык здесь — **средство**,
> чтобы протокол проверялся компилятором, а не соблюдался по доброй воле. Имя читается двояко и
> оба чтения верны: `NOL + lang` и **`no lang`** — программ в обычном смысле тут не пишут.
> Ниже слово «язык» означает именно средство (грамматику, типы, машину), а не жанр.

*Проверено: `src/theta.lisp` + `test/99_theta.lisp` — **22 проверки**, включая имитацию
гарантии conformal на 16 000 испытаний.*

---

## 0. Почему это вообще вопрос

В `ПРИМЕРЫ_v0` стояло `:requires (>= belief 0.9)`. Откуда девять десятых? Ниоткуда. А порог,
взятый с потолка, отменяет всю строгость вокруг него: можно сколь угодно точно считать веру и
затем сравнить её с числом, которое кто-то придумал.

Наша же статья («Свидетель без субстанции», §9) честно называет три линии предшественников и
признаёт, что связка с ними **не оценивалась**. Невис разобрала их роли, и оказалось, что это
**не три кандидата на одно место**.

| линия | на какой вопрос отвечает | берём ли в фундамент |
|---|---|---|
| **Arrow–Fisher (1974)**, quasi-option value | **что значит** порог: премия за необратимость | **да — смысл** |
| **Conformal risk control** | **как выставить**, не веря своим числам | **да — процедура** |
| **Chow (1970)**, reject option | предок вопроса | **нет** — требует калиброванной вероятности |

🔴 **Почему Chow не в фундамент.** Ему нужна калиброванная апостериорная вероятность. Наша `c`
не калибрована и **по построению не может быть**: `c = w/(w+k) < 1` структурно — это граница
Эйн-Соф. Строить на нём значило бы притвориться, что у нас есть то, чего у нас нет по замыслу.
Держим как историю вопроса.

---

## 1. Смысл: θ из ценности сохранённой возможности

Действие необратимо. Альтернатива — **свернуться и узнать больше**.

```
G  выигрыш, если утверждение истинно
L  потеря, если ложно (необратимая — откатить нельзя)
λ  вероятность узнать правду, если подождать     ← сколько несёт ожидание
δ  во сколько ценится отложенный исход           ← 0: будущее ничего не стоит; 1: как настоящее
```

Ценность действия сейчас: `V = b·G − (1−b)·L`.
Ценность ожидания: `δ·[ λ·b·G + (1−λ)·V ]` — узнав правду, действуем **только при истине**.

Действовать выгоднее ожидания ⟺ `V ≥ δ[λbG + (1−λ)V]`. Раскрываем:

```
V(1 − δ(1−λ)) ≥ δλbG                     ⇒   K := δλ / (1 − δ(1−λ))
bG − L + bL ≥ K·bG
b(G + L − KG) ≥ L
```

> ### 🔴 θ = L / ( G·(1−K) + L ),  K = δλ / (1 − δ(1−λ))

`K` — квази-опционная надбавка Эрроу–Фишера, выраженная долей: **сколько выигрыша съедает
уничтоженная возможность подождать**. Именно её не учитывает наивное `b·G > (1−b)·L`.

### Пределы (каждый проверен отдельно)

| условие | K | θ | смысл |
|---|---|---|---|
| `λ=0` — ожидание не приносит сведений | 0 | `L/(G+L)` | классический порог; ждать незачем |
| `δ=0` — будущее ничего не стоит | 0 | `L/(G+L)` | то же: отложенный выигрыш обнулён |
| `λ=1, δ=1` — **даровое точное знание** | 1 | **1** | необратимое **не оправдано никогда** |
| `L→0` | — | `→0` | нечего терять — действуй |
| `L≫G` | — | `→1` | почти всё поставлено на кон |

🔴 **Проверка пределов поймала мою ошибку.** В первом наброске страж знаменателя был написан
на неверное условие, и случай «даровое точное знание» дал `0.5` вместо `1`. Предельные случаи —
не украшение вывода, а его проверка.

### Связь с классом обратимости

`λ` — это и есть **мера того, насколько ценно подождать**, а она задаётся классом действия:

```
обратимое     λ=0.0  →  θ = 0.500     откатить можно, ждать нечего
возместимое   λ=0.3  →  θ = 0.787     откат стоит денег
НЕОБРАТИМОЕ   λ=0.6  →  θ = 0.865     ждать особенно ценно — окно закроется навсегда
```

Три класса обратимости, которые в языке уже были, **получили численный смысл**: они не ярлыки,
а разные значения `λ`, и потому разные пороги.

### 🔴 Граница Эйн-Соф работает и здесь

`b = f·c < 1` **структурно** (доказано: `b<1` в `formal/BeliefMass.agda`). Значит `θ = 1`
**недостижим ни одной верой**. Отсюда следствие, которое стоит сказать вслух:

> Политика «дождаться полной уверенности перед необратимым» **не действует никогда**.

Конечный носитель не может ждать достоверности — её не будет. Порог обязан быть строго меньше
единицы не из смелости, а из конечности. То, что мы держали как красивое совпадение математики
и богословия, здесь тоже **несёт вес в выводе**, а не стоит эпиграфом.

---

## 2. Процедура: θ из журнала, без веры в собственные числа

💎 **Журнал и есть калибровочная выборка.** `Λ` хранит массу веры каждого совершённого действия;
когда исходы становятся известны, накапливается набор «вера при утверждении, оказавшемся ложным».

Conformal risk control **не требует**, чтобы наши `b` были калиброванными вероятностями —
достаточно **обмениваемости** прошлых случаев с будущими. Это ровно то, чего нам не хватало
у Chow.

**Цель:** доля ложных утверждений, прошедших гейт, не выше `α`.
**Способ:** берём `(1−α)`-квантиль вер у заведомо ложных прошлых случаев, с поправкой `(n+1)`.

```lisp
(theta-conformal ложные-веры alpha)
```

**Проверено имитацией**, а не доверием формуле: 16 000 испытаний на каждый уровень.

| α | доля ложных, прошедших гейт | 3σ-предел |
|---|---|---|
| 0.05 | 0.0502 | 0.0552 |
| 0.10 | 0.0954 | 0.1071 |
| 0.20 | 0.2002 | 0.2095 |

🔴 **Первая редакция теста объявила метод нарушенным** при доле `0.0510` против `α=0.05`. Это
было **0.4σ, то есть шум**: гарантия conformal маргинальна (в среднем по калибровочной выборке),
и сравнивать голую долю с `α` без допуска нельзя. **Врал тест, не метод.** Теперь допуск 3σ
и он назван прямо.

🔴 **Мало данных → `θ = 1`, то есть отказ, а не догадка.** Пустая калибровочная выборка не даёт
права на риск — ровно та же дисциплина, что у пустой посылочной базы (`∅ ↦ ⊥`, а не `⊤`).

---

## 3. Вместе: побеждает осторожнейший

```lisp
(theta-both G L λ δ ложные-веры α)  ≡  (max (theta-derived …) (theta-conformal …))
```

Вывод говорит, каким порог **должен быть** по цене ошибки; калибровка — каким он **обязан быть**,
чтобы прошлое не повторилось. Спорить им не о чем: берётся более строгий.

---

## 4. В языке

```
irreversible action publish
  gated by belief >= derived gain 1 loss 4 learn 0.6 discount 0.9
  else fold
```

θ считается **при разборе** — но **вывод остаётся в исходнике**. Читатель видит не голое `0.962`,
а из чего оно получилось. Неполный вывод не разбирается: назвать выигрыш и умолчать о потере
нельзя.

Число по-прежнему принимается — вывод **предпочтителен, а не обязателен**. Заставлять всех
выводить θ значило бы мешать там, где цена ошибки очевидна; но «взято с потолка» перестало быть
умолчанием.

Наглядно, на одном и том же случае:

```
gain 1 · loss 4 · learn 0.6 · discount 0.9   →  θ = 0.962
то же, но ожидание бесполезно (learn 0)      →  θ = 0.800
разница 0.162 — и есть премия за необратимость
```

---

## 5. Что здесь открыто

- **`λ` и `δ` пока объявляет автор.** Вывод честен относительно них, но сами они — оценки.
  Следующий шаг: брать `λ` из истории (как часто ожидание действительно приносило решающего
  свидетеля) — журнал и для этого годится.
- **Калибровка требует известных исходов.** Языку нужна форма «это утверждение оказалось ложным»,
  чтобы журнал сам становился выборкой. Пока связь ручная.
- **Обмениваемость предполагается, а не проверяется.** Если корпус меняется по существу, прошлые
  случаи перестают быть обмениваемыми с будущими, и гарантия слабеет. Проверки на это нет.
- **Chow не сопоставлен формально** — только отведён по основанию «нужна калибровка». Полное
  сравнение осталось в долге, как и признавала наша статья.
