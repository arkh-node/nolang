;;;; nolang src — nolang. ЕДИНАЯ ТОЧКА ВХОДА.
;;;;
;;;; 🔴 Зачем: модули образуют РОМБ — `parse` → `check` и `reduce` → `check`, общей вершины
;;;; нет. Кто грузил только `reduce.lisp`, оставался без разборщика и узнавал об этом
;;;; «undefined function» посреди прогона (поймано тестом носителя 28.07). Одна точка входа
;;;; снимает вопрос «что грузить», не сливая при этом сами слои.
;;;;
;;;; ⚠️ Порядок важен: `check` грузится дважды (из обоих), и это законно — переопределение
;;;; идентично. Слить `parse` и `reduce` в один файл НЕЛЬЗЯ по той же причине, что и всегда:
;;;; раздельность `chk-claim`/`red-claim` несёт доказательство (см. шапку common.lisp).
(load (merge-pathnames "parse.lisp" *load-pathname*))
(load (merge-pathnames "reduce.lisp" *load-pathname*))
