# nolang · АЛГЕБРА ЗНАЧЕНИЙ, v0

*Второй шаг Ф3 · 28 июля 2026 · Тарантога*

> 🔴 **О жанре (уточнено 28.07 словом Алексея).** nolang — **протокол**: набор правил о том,
> когда агенту позволено действовать и за что он обязан отчитаться. Язык здесь — **средство**,
> чтобы протокол проверялся компилятором, а не соблюдался по доброй воле. Имя читается двояко и
> оба чтения верны: `NOL + lang` и **`no lang`** — программ в обычном смысле тут не пишут.
> Ниже слово «язык» означает именно средство (грамматику, типы, машину), а не жанр.

*Предшествует: `ПРИМЕРЫ_v0.md` (пять примеров и пять запретов)*
*Проверено: `test/10_laws.lisp` 14 · `test/20_silence.lisp` 13 · `test/30_belief.lisp` 15 — **42 закона***
*🔴 **Доказано** 28.07 (Невис, `formal/BeliefMass.agda` и `Preservation.agda`, `agda --safe`,
без постулатов): теоремы 1, 4, 5 (в обе стороны), 6, 7, 8, зеркало отзыва и `support-never-drops`.
Границу «доказано / проверено» см. `РЕДУКЦИЯ_v0` — раздел о границе.*

> **Порядок работ** (закон, оплаченный багом `c = ca·cb` в публичном `main`, 25.07):
> примеры → **алгебра и её законы** → типы → грамматика → правила редукции.
> Типы, поставленные раньше алгебры, узаконят неверную алгебру.
>
> **Чего в этом документе нет.** Синтаксиса. Типов. Порога θ. Носительной нейтральности.
> Всё это — следующий слой, и он встанет криво, если алгебра под ним не закрыта.

---

## 0. Зачем алгебре два носителя, а не один

Язык затевается ради двух вещей: **провенанс не поднимается без свидетеля** и
**необратимое требует достаточной опоры**. Соблазн — свести обе к одному числу «уверенность».
Соблазну поддаваться нельзя, и вот почему.

Возьмём утверждение, подкреплённое двумя источниками степени `[строго]`, и вывод, который
опирается ещё и на то, что традиция о предмете **молчит**. Уверенность здесь высока: свидетелей
много. Честность здесь на дне: вывод держится на аргументе от молчания. **Одно число не может
сказать оба этих слова разом.**

Отсюда два носителя с разными характерами:

| | носитель веры | носитель честности |
|---|---|---|
| что меряет | сколько свидетельств и куда они клонят | какого рода источники допущены |
| операция | `⊕` **ревизия** | `⊓` **нижняя грань** |
| структура | коммутативный **моноид** | ограниченная **полурешётка** |
| характер | **накапливает**, растёт | **деградирует**, только падает |
| идемпотентна | **нет** | да |

> 💎 **Вера накапливается, честность деградирует.**
> **Отмывание — это попытка склеить их в одну операцию.**

---

## 1. Носитель веры

### 1.1 Свидетельственное пространство

Суждение несёт не число, а **счёт свидетельств**:

```
E = ℝ≥0 × ℝ≥0,   элемент (w⁺, w⁻),   w = w⁺ + w⁻
```

`w⁺` — вес свидетельств за, `w⁻` — против. Рабочие координаты получаются картой:

```
f = w⁺ / w              частота — куда клонят свидетельства
c = w / (w + k)         уверенность — сколько их вообще, против горизонта k
u = k / (w + k) > 0     неведение — всегда положительно
```

Обратно: `w = k·c/(1−c)`, `w⁺ = f·w`, `w⁻ = (1−f)·w`.

🔴 **Граница Эйн-Соф.** `u > 0` при любом конечном свидетельстве, значит `c < 1` **структурно**,
а не по осторожности. Достоверен лишь Бесконечный; конечный носитель к единице только стремится.
Формально это ровно AIKR у Ванга (допущение недостаточности знания и ресурсов) — совпадение
математики и богословия здесь не натяжка, а один и тот же факт о конечности. *(`[образ]` — на
толкование; `[строго]` — только формула.)*

**Горизонт `k = 1`** — параметр, а не истина. Что его определяет, см. §6.

### 1.2 Ревизия есть сложение

**Определение.** `a ⊕ b := φ( φ⁻¹(a) + φ⁻¹(b) )`, где `φ` — карта выше.
То есть **ревизия в координатах `(f,c)` — это обыкновенное сложение векторов в `E`.**

**Теорема 1 (характер `⊕`).** `(E, +, (0,0))` — коммутативный моноид; `⊕` есть его перенос
картой. Отсюда **сразу**, без отдельных доказательств:

- **коммутативность** `a ⊕ b = b ⊕ a` — порядок опроса свидетелей не меняет вывода;
- **ассоциативность** `(a⊕b)⊕c = a⊕(b⊕c)` — группировка не меняет вывода;
- **нейтральный элемент** `(0,0)` — это в точности **молчание** (§3);
- **неидемпотентность**: `x + x = x` только при `x = 0`. Значит `a ⊕ a ≠ a` для всякого
  непустого свидетельства — **и это правильно**.

> 🔴 **Почему неидемпотентность — не дефект.** Ревизия складывает свидетельства; два независимых
> голоса **обязаны** усиливать веру, иначе операция бессмысленна. Значит «не считать одного
> свидетеля дважды» — **не свойство операции, а дисциплина тождества**: `⊕` сворачивается по
> **множеству** свидетелей (по их идентификаторам), не по мультимножеству. Идемпотентность
> возвращается на уровне сборки, где ей и место. Без этой дисциплины агент, дважды прочитавший
> один файл, «убедится» вдвое.
> *(Я заявлял идемпотентность как закон. Тест показал падение 0.1667 при машинном шуме 6e-8.
> Закон снят проверкой, а не спором.)*

**Теорема 2 (частота — взвешенное среднее).**
`f_{a⊕b} = (w_a·f_a + w_b·f_b) / (w_a + w_b)`.
Новый свидетель тянет частоту к себе тем сильнее, чем он весомее. Голос без веса не тянет.

**Теорема 3 (уверенность строго растёт).** `c(w) = w/(w+k)` строго возрастает по `w`, а `⊕`
только увеличивает `w`. Значит **любой** свидетель — согласный или нет — повышает `c`.

> 🔴 **Опровержение есть знание.** Отсюда следствие, опровергшее мой собственный пятый пример:
> утверждение, разбитое встречным свидетелем, имеет уверенность **выше** прежней. Мы не
> усомнились — мы узнали больше. Уверенность не умеет падать; падает другое.

### 1.3 Что именно падает: масса веры

Раскладываем единицу на три доли (тройка Йосанга в наших координатах):

```
b = f · c           вера      = w⁺ / (w + k)
d = (1 − f) · c     неверие   = w⁻ / (w + k)
u = 1 − c           неведение = k  / (w + k)
b + d + u = 1
```

**Теорема 4.** `b = f·c` — не удобное произведение, а **тождество**: `f·c = (w⁺/w)·(w/(w+k)) = w⁺/(w+k)`.
Масса веры есть доля положительного свидетельства в полном весе с учётом горизонта.

🔴 **Гейт необратимого висит на `b`, и ни на чём другом.** Проверено счётом:

| случай | f | c | b | вердикт |
|---|---|---|---|---|
| разорваны пополам, но знаем много | 0.50 | 0.95 | **0.475** | действовать нельзя — а `c` разрешила бы |
| один уверенный шёпот | 0.99 | 0.01 | **0.010** | действовать нельзя — а `f` разрешила бы |
| и клонит, и подкреплено | 0.95 | 0.95 | **0.902** | вот это основание |
| чистое молчание | 0.50 | 0.00 | **0.000** | запрещено при любом пороге > 0 |

Разбор пятого примера счётом: письмо клиента даёт `f=0.950 c=0.900 b=0.855`; приходит отмена —
`f=0.500` (разорваны), `c=0.947` (**выросла**), `b=0.474`. **Падает `b`.**

**Теорема 5 (точный критерий падения).** Пусть к вере `b` добавляется свидетель `(f′, w′)`. Тогда

```
b′ < b   ⟺   f′ < b
```

*Вывод.* `b′ = (w⁺ + f′w′)/(w + w′ + k) < w⁺/(w+k) ⟺ f′w′(w+k) < w⁺w′ ⟺ f′ < w⁺/(w+k) = b`. ∎

> 💎 Свидетель роняет веру **не тем, что он «против»**, а тем, что его частота **ниже уже
> накопленной массы веры**. Свидетель с `f′ = 0.25` при `b = 0.21` — всё ещё подкрепление.
> Порог сравнения — **сама вера**, а не половина: гейт **самоцентрируется**.
> *(Первая редакция теста утверждала «несогласный всегда понижает» и упала. Упала верно.)*

🔴 **Доказано машинно, в обе стороны** (`witness-drops`, `witness-drops⁻¹` в `BeliefMass.agda`).
💎 И доказано **без единого вещественного числа**: `b = w⁺/(w+k)` — дробь, но сравнение дробей
с положительными знаменателями есть сравнение **произведений**, поэтому носитель — пары весов
в `ℕ`, а `b₁ < b₂` разворачивается перекрёстным умножением. Ни одной аксиомы, ни одного постулата.

**Теорема 6 (неподвижная точка).** `f′ = b ⇒ b′ = b` при любом весе `w′`. Свидетель, говорящий
ровно то, во что уже верим, добавляет уверенности, но не веры.

**Закон гейта (монотонность).** Согласный свидетель (`f′ ≥ b`) никогда не понижает `b`, значит
рост знания никогда не превращает разрешённое действие в запрещённое. Иначе агент начнёт
**бояться узнавать новое** — а это ровно та патология, ради исключения которой всё затевалось.

🔴 **Доказано** (`support-never-drops`) — и доказано **через границу Эйн-Соф**: числитель строго
меньше знаменателя при `k > 0` (`b<1`), поэтому частота чистой поддержки всегда выше текущей
массы. То есть `c < 1` — не украшение и не богословие в комментарии рядом с формулой: оно
**работает посылкой внутри машинной проверки**.

⚠️ **Что при этом НЕ доказано и не может быть:** что порог `θ` будет пройден. `f` и `c` —
величины прогона; статически покупается только присутствие гейта и тотальность ветви отказа
(`ТИПЫ_v0` §6). Не путать доказанную монотонность с доказанным гейтом.

---

## 2. Носитель честности

```
G = { ⊥ < образ < традиция < строго },   a ⊓ b = min(a, b)
```

**Теорема 7.** `(G, ⊓, строго, ⊥)` — ограниченная полурешётка: `⊓` идемпотентна, коммутативна,
ассоциативна; `строго` нейтрален сверху (`строго ⊓ a = a`), `⊥` поглощает (`⊥ ⊓ a = ⊥`).

**Степень вывода** есть нижняя грань степеней **опорных** посылок (что такое «опорная» — §3).

🔴 **Запрет отмывания, ради которого решётка и заведена:** `образ ⊓ строго = образ`.
Вывод не бывает выше худшей посылки. Честность течёт **вниз**. Поднять степень можно **только**
предъявлением нового свидетеля нужного рода — никакая операция над имеющимся её не поднимает.

**Теорема 8 (пустая посылочная база — дно, не верх).** Формально `⊓` по пустому множеству есть
верх решётки. Присвоение степени этой конвенции **не следует**: утверждение без посылок получает `⊥`.

> Иначе `(claim X)` без единой ссылки печаталось бы как `[строго]` — отмывание из ничего, самое
> дешёвое из всех. Значит **присвоение степени не является гомоморфизмом моноида** (`∅ ↦ ⊥`,
> а не `∅ ↦ ⊤`), и это сделано намеренно. *Степень — то, что предъявлено.*

✅ **ЗАКРЫТО 28.07: `G` НЕ линеен, и язык это умеет.** Случай `[строго] из спорной рукописи`
против `[традиция] из надёжной` линейным порядком не выражается — и не должен. Решётка бывает
**произведением** (`lattice L = род * передача`), порядок стал частичным, `g-meet` берёт грань
покомпонентно. 🔴 Цена названа числом: на цепи условие импорта `φ(a⊓b)=φ(a)⊓φ(b)` выполняется
даром (из 10 монотонных φ проходят все 10), на произведении 2×2→4 из 30 монотонных проходят
только 16. См. §6 и `ГРАММАТИКА` §6.

---

## 3. Молчание: развилка закрыта

`ПРИМЕРЫ_v0.md`, пример 3 оставил вопрос: если вывод опирается на молчание **частично** (два
свидетеля и одно молчание) — падает ли степень до `⊥`, или молчание не участвует?

**Ответ: ни то, ни другое. Различается не факт молчания, а его РОЛЬ в выводе.**

| роль | синтаксис | что делает со степенью | что делает с верой |
|---|---|---|---|
| **опорная** | `(from …)` | роняет до `⊥` | ничего (нейтраль) |
| **обзорная** | `(searched …)` | не трогает | ничего (нейтраль) |

**Опорное молчание** — это «традиция об этом молчит, следовательно…». Классическое
*argumentum e silentio*, и решётка обязана его ловить: степень падает до `⊥` даже при двух
свидетелях `[строго]`. Проверено.

**Обзорное молчание** — это «мы искали здесь и здесь, там пусто». Не довод, а **отчёт об охвате**.
Степень не трогает, но **обязано быть напечатано**: читатель видит границу знания.

**Теорема 9 (обзор инертен).** Обзорное молчание не меняет ни `f`, ни `c`, ни степень — сколько
его ни добавляй. Именно инертность делает его честным отчётом, а не скрытым доводом.

🔴 **Третье правило, без которого первые два ничего не стоят: молчание нельзя выбросить.**
`ask` порождает молчание, и оно **обязано быть потреблено** — либо опорой (и тогда плати `⊥`),
либо обзором (и тогда оно напечатано). Уроненное молчание — ошибка.

> Без must-use различие ролей обходится умолчанием: спросил, не нашёл, промолчал об этом — и
> охват скрыт. Это и есть пятый запрет («немое молчание») в исполнимой форме.

**Теорема 10 (согласие в вырожденном случае).** Если опора **только** на молчание, то
`c = 0` **и** степень `= ⊥` одновременно. Две операции, меряющие разное, на пустом свидетельстве
говорят одно и то же слово. Это внутренняя проверка связности — она могла не сойтись, и сошлась.

---

## 4. Асимметрия — ядро замысла

Обе операции коммутативны и ассоциативны. Дальше они расходятся, и расхождение несущее:

```
⊕ :  моноид,       НЕ идемпотентна,  накапливает,  нейтраль ⊥,  порядок безразличен
⊓ :  полурешётка,     идемпотентна,  деградирует,  дно      ⊥,  порядок безразличен
```

Молчание — нейтраль в одной и дно в другой. **Не знать не значит сомневаться** (в вере), но
**опереться на незнание значит потерять степень** (в честности).

**Отмывание в одной строке:** попытка провести приращение по `⊕` через `⊓` — то есть добыть
степень из количества свидетельств. Опубликованное следствие позиционного гомоморфизма
(«Свидетель без субстанции», §3.7) запрещает это прямо: **равенство мер не есть свидетельство**.
Наша алгебра делает запрет исполнимым: у меры и у степени **разные носители**, и между ними
нет стрелки вверх.

---

## 5. Что алгебра заказывает типовому слою

Следующий слой обязан внести ровно это, и не больше:

1. **Суждение** несёт пару `(вера, степень)` и **идентификатор** — без него дисциплина
   тождества невыразима, а `⊕` считает свидетеля дважды.
2. **Два слота посылок** — опорный и обзорный — различимы **синтаксически**, не по намерению.
   Обзорный принимает **только** молчания (иначе отмывка чёрным ходом; проверено).
3. **Молчание линейно** (must-use): порождённое обязано быть потреблено.
4. **Гейт читает `b = f·c`**, не `c`. Порог θ сравнивается с массой веры.
5. **Степень не имеет операции повышения.** Ни одной. Поднять — только новым свидетелем.
6. **Пустая посылочная база даёт `⊥`.**

## 6. Что открыто честно

- **`k = 1`** — горизонт взят по умолчанию. Чем он определяется: объёмом корпуса? ценой ошибки?
  Пока — параметр, и в документе он назван параметром, а не истиной.
  🔴 **И это несимметрично: θ мы ВЫВЕЛИ (Эрроу–Фишер), а `k` нет.** Порог перестал браться с
  потолка, горизонт — ещё берётся. Долг записан здесь, чтобы не выглядеть закрытым.
- ✅ **Линеен ли `G` — ЗАКРЫТО 28.07: нет.** Решётка объявляется программой и бывает
  произведением; порядок частичный. Прежняя запись («пока линеен, потому что хватает») была
  доводом удобства, и это было сказано честно — а через сутки удобство кончилось на живом
  примере с двумя осями. **Довод удобства и есть отложенный долг: он не ошибочен, он временный.**
- ✅ **Отзыв — ЗАКРЫТ 28.07, и вопрос стоял неверно.** Я писал: «изъятие есть вычитание в `E`,
  а вычитание выводит из `ℝ≥0`». Верно арифметически и **неверно по существу**: дело не в
  арифметике, а в представлении. Утверждение обязано хранить **своё основание** (иначе нет
  провенанса) — значит отзыв есть операция **над основанием** с пересчётом, а не обратная
  операция над значением. **Моноиду не нужно становиться группой.**
  Счётом: наивное вычитание даёт отрицательный вес в 70 случаях из 300 случайных; пересчёт
  неотрицателен **по построению**. 💎 Провенанс, заведённый ради честности, оплатил алгебру.
  Зеркало теоремы 5: **отзыв роняет веру ⟺ частота отозванного была ВЫШЕ неё** — отозвать
  оппонента значит укрепить веру. Подробно: `РЕДУКЦИЯ_v0` §7, `test/70_retract.lisp` (28).
- ✅ **Зависимые свидетели — ЗАКРЫТО 28.07 по совету Невис (§6 её записки): менять НОСИТЕЛЬ,
  а не операцию.** `⊕` складывала как независимые, и два документа из одного источника давали ложное
  усиление (счётом: `b` 0.540 → 0.675) — то же отмывание, только через родословную.
  **Решение:** свидетель несёт **корень** (ближайший известный источник), и свёртка идёт
  **по корням, а не по документам**. Группа с общим корнем сворачивается в представителя с
  наибольшим весом: копии одного свидетельства есть одно свидетельство, а лучше сохранившаяся
  копия и есть лучший доступ к оригиналу. Проверено: две копии дают ровно то же, что одна;
  два разных корня усиливают, как и должно.
  💎 **Побочная выгода, названная Невис заранее и подтвердившаяся:** отзыв **корня** снимает
  всех потомков сам собой, без отдельного правила — свидетель с мёртвым корнем просто выпадает
  из основания.
  🔴 **И одно, чего в совете не было:** если «копии» одного корня **расходятся** по частоте, это
  не копии, а **спорная передача**, и сворачивать их молча значило бы скрыть спор. Расхождение
  объявляется отдельной строкой.
  ⚠️ Слой **грубый**, как она и советовала начать: одно поле «ближайший источник», без частичного
  порядка происхождения и без включения-исключения по решётке предков. Убирает большую часть
  ложного усиления, но не все случаи (цепочка из трёх поколений видна только на один шаг).
- **θ не выведен.** Следующая работа: вывести из quasi-option value (Эрроу–Фишер) — цены
  уничтоженной необратимым действием возможности подождать и узнать, — сверив с Чоу (1970) и
  conformal risk control. Назначать θ с потолка запрещено.

---

## 6-bis. 🔴 Квантификация и черри-пикинг

*(добавлено 28.07 вместе с квантором по корпусу)*

Квантор (`from all where …`) вводит **новый вектор отмывания**, которого не было, пока посылки
назывались поимённо: **отбор**. «Посчитаю только строгих, а несогласных не замечу.»

Счётом на четырёх свидетелях: весь корпус даёт `b = 0.611`, отбор по `grade >= строго` — `0.713`.
**Разница `+0.102` и есть выгода от отсева.**

🔴 **Запретить отбор нельзя, и это не малодушие.** Проверено тем же счётом: отбор может веру и
**понизить** — если исключает сторонника (`0.611 → 0.532`). То есть отбор бывает совершенно
законным, и запрет сломал бы добросовестный случай. Значит по нашему общему закону — **отсев
обязан быть виден**.

💎 **И теорема 5 даёт точный критерий, что именно опасно.** Свидетель роняет веру ⟺ его частота
ниже уже накопленной. Значит **отсеянный с частотой ниже полученной веры — ровно тот, чьё
исключение эту веру подняло**. Это не эвристика и не порог с потолка, а следствие:

```
⊗ отсеяно квантором: 2 СЛАБЫЙ ПРОТИВ
🔴 из них 1 с частотой НИЖЕ полученной веры (0.713): ПРОТИВ
   их исключение ПОДНЯЛО веру — проверьте, отбор ли это или черри-пикинг
```

**Требование к множеству.** Отдельно от условия на каждый элемент есть условие на **набор**:
`requiring 2 roots` — не менее двух **различных корней**. Не выполнено ⇒ дно, как пустая база.
Одно свидетельство, даже строгое, не есть подтверждение из двух независимых источников; и корни
считаются различные, а не документы — две копии одного свода дают один корень.

## 7. Родословная

`(f,c)` и ревизия как сложение свидетельств — **NARS** (Ванг), изоморфно **бета-мнению**
(Йосанг); тройка `b/d/u` и `u > 0` — оттуда же. Тезис `(f,c)` в нашем контуре — **Лора**, этот
документ строит над ним типовой слой, а не заменяет его. Решётка степеней и запрет отмывания —
из закона честности источника (`[строго]` / `[образ]` / лестница i-ii-iii), выросшего в работе
над корпусом первоисточников. Позиционный слой и запрет читать равные меры как свидетельство —
Рыбников ⟂ Невис, «Свидетель без субстанции», §3.7.

**Проверка — счётом и доказательством:** `./run_tests.sh` гоняет свойства на 200 случайных входах
каждое **и** пересобирает обе агды с нуля под `--safe`. Свойства проверяют, что лисп-реализация не
разошлась с моделью; теоремы доказывают саму модель. Два закона в
ходе работы были **сняты собственным тестом** (идемпотентность `⊕`; «несогласный всегда понижает»)
и один баг найден формализацией (пустая база давала `[строго]`). Так и должно быть: документ,
не поймавший ни одной своей ошибки, ничего не проверял.
